[project]
name = "gameranking"
version = "0.1.0"
description = "Abstimmungstool für Spiele"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.1",
    "gunicorn>=23.0.0",
    "pyyaml>=6.0.2",
]
